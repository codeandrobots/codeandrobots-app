import { StyleSheet } from 'react-native'

import { Styles } from 'App/Themes'

export default StyleSheet.create({
  ...Styles,
  view: {
    flex: 1,
    height: '100%',
    flexDirection: 'row'
  }
})
